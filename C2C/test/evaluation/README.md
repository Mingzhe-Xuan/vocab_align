# Evaluation tests

This folder validates cross-method result analysis built on the unified
per-sample evaluation schema. Tests use synthetic local records and never load
models, download data, or require a GPU.
